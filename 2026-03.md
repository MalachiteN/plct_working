# 2026年3月

## PR

[fix(venv): empty sysroot input should use toolchain default, not skip by MalachiteN · Pull Request #128 · ruyisdk/ruyisdk-vscode-extension](https://github.com/ruyisdk/ruyisdk-vscode-extension/pull/128)

澄清了 venv 创建过程中，指定 sysroot 时的 UI/UX 状态机：按下 ESC 显式拒绝设置任何 sysroot 路径，输入非空字符串显式设置一个特定的 sysroot 路径，而输入空字符串（直接按下 Enter）表示以工具链默认 sysroot 创建虚拟环境，解决了用户无法以工具链默认 sysroot 创建虚拟环境的问题。

**[被关闭]** [feat(ruyipkg): add porcelain support to &#39;ruyi list profile&#39; by MalachiteN · Pull Request #432 · ruyisdk/ruyi · GitHub](https://github.com/ruyisdk/ruyi/pull/432)

主动与上游 ruyi 交流如何彻底解决 ruyi list profiles 没有健壮的机器友好输出的问题，尝试提供一个 porcelain 模式实现被拒绝，但在交流过程中了解了全新的 entity 系统

[refactor(venv): use structured profiles list from entity system by MalachiteN · Pull Request #129 · ruyisdk/ruyisdk-vscode-extension](https://github.com/ruyisdk/ruyisdk-vscode-extension/pull/129)

确认到 experimental 标志的功能对下游开发者可视为稳定功能之后，重构 ruyisdk vscode 插件以读取 entity 系统的机器友好 profile 列表（JSON），彻底解决了正则解析 ruyi list profiles 人类友好输出的脆弱性。

## 解决的 issues

[虚拟环境创建时添加sysroot选项失败 · Issue #127 · ruyisdk/ruyisdk-vscode-extension](https://github.com/ruyisdk/ruyisdk-vscode-extension/issues/127)
